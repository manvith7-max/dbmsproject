# Use localhost for a local MySQL install.
# Use db when running with Docker Compose.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=hostel_db
JWT_SECRET=your_jwt_secret
PORT=3000
